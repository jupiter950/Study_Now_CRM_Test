const Application = require('./application.model');
const Document = require('./document.model');
const Note = require('./note.model');
const TransitionLog = require('./transition-log.model');
const AiAssessment = require('./ai-assessment.model');

module.exports = {
  Application,
  Document,
  Note,
  TransitionLog,
  AiAssessment,
};
